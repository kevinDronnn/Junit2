public class Words {
    public String word(String str2) {
        String str1="Hello";

        if (str1.equals(str2)){
            System.out.println("Two strings are the same!!!");
        }else {
            System.out.println("Strings are not the same!!!");
        }
        return str1;
    }
}
